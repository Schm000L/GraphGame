/*
export const ColumnsActions = "CHANGE_COLUMNS"

export const changeColumns = (columns: number) => ({
    type: 'CHANGE_COLUMNS',
    columns: columns
  })

const ColulmnsReducer = (action: typeof ColumnsActions ) => {
    switch (action) {
      case 'CHANGE_COLUMNS':
        (columns: number) => {
            return 
        }
      default:
        return columns
    }
}*/